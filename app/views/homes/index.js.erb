console.log('This is from the server, NIIIICE. there are <%= @homes.size %> homes matched')

<% if @homes.any? %>
  $('.homes-list-group').html('<%= j(render @homes) %>')
<% else %>
  $('.homes-list-group').html('<%= j(render 'nohomes' ) %>')
<% end %>
